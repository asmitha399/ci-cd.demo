name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Show working directory and files
        run: |
          pwd
          ls -la

      - name: Install dependencies
        run: npm install --verbose

      - name: Build application
        run: |
          echo "Building the application..."
          # Replace this with your actual build command:
          # npm run build

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: |
          echo "Running tests..."
          # Replace this with your actual test command:
          # npm test

  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy the application
        run: |
          echo "Deploying the application..."
          # Add your real deploy script here, e.g.:
          # scp files to a server, or use a CLI like Firebase, Netlify, etc.

      

  

          

  
